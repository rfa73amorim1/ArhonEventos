@model IEnumerable<AthonEventos.Models.Certificado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@using (Html.BeginForm())
{
    <p>
        Find by name: @Html.TextBox("SearchString")
        <input type="submit" value="Search" />
    </p>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PalestraID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UsuarioID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CertificadoDt)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Palestra.PalestraName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CertificadoDt)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CertificadoID }) |
                @Html.ActionLink("Details", "Details", new { id = item.CertificadoID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.CertificadoID })
            </td>
        </tr>
    }

</table>
